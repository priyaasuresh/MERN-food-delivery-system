/*import React, { Component } from 'react';

class AddToCart extends Component {
  constructor(props){super(props);
     this.state = {counter:0 };
   }

  increment(){
    this.setState({counter: this.state.counter + 1});}

  decrement(){
      if(this.state.counter > 0){ this.setState({ counter: this.state.counter - 1});}
    }
  render() {
    return (
      <div>
        <div id="center">
            <label for="name">CHICKEN BIRYANI</label>
            <button type ="button" className="btn btn-success" id="minus" onClick={this.decrement.bind(this)}>-</button>
            <input type="text" id="date" value={this.state.counter}/>
            <button type="button" className="btn btn-danger"  id="plus" onClick={this.increment.bind(this)}>+</button>
        </div>
      </div>
    );
  }
}

export default AddToCart;


*/